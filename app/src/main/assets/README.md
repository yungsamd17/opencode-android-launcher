# Placeholder for shell scripts (Phase 1+).
#
# Per AGENTS.md conventions: keep shell logic as plain files in assets/,
# don't hand-translate shell logic into Kotlin string concatenation.
# RuntimeBootstrap will copy these out of assets and exec via ProcessBuilder.
